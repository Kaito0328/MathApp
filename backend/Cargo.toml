# (ルート)/Cargo.toml

[workspace]
# このワークスペースに含まれるクレートのパスを指定する
members = [
    "crates/linalg",
    "crates/coding",
    "crates/utils",
    "crates/signal_processing",
]

# リリースビルドの最適化設定などをここに集約できる
[profile.release]
lto = true
opt-level = 3