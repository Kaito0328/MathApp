// use signal_processing::iir::iir_filter;

// fn approx_eq_vec(a: &[f64], b: &[f64], eps: f64) {
//     assert_eq!(a.len(), b.len());
//     for i in 0..a.len() {
//         let da = a[i];
//         let db = b[i];
//         let diff = (da - db).abs();
//         assert!(diff <= eps, "idx {i}: {da} vs {db} (|diff|={diff})");
//     }
// }
